---
layout:     post
title:      Hash
subtitle:   Hash表的练习
date:       2026-01-27
author:     ZH
header-img: img/home-bg-geek.jpg
catalog: true
tags:
    - Algorithm
---

383.赎金信 link:https://leetcode.cn/problems/ransom-note/submissions/694430733/

hash表练习，记录相应的出现次数
通过记录各个字母的出现次数，然后再匹配中每匹配一次就减一，以此查看ransomNote的单词能否有magazine里面的字母重组成

    def canConstruct(self, ransomNote: str, magazine: str) -> bool:
        if  (ransomNote in magazine) or (ransomNote in magazine[::-1]):
            return True
        hash = {}
        for m in magazine:
            if m not in hash:
                hash[m] = 0
            hash[m] += 1
        for r in ransomNote:
            if hash.get(r, 0) == 0:
                return False
            else:
                hash[r] -= 1
                continue
        return True

